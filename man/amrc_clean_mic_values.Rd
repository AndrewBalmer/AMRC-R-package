% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mic_data.R
\name{amrc_clean_mic_values}
\alias{amrc_clean_mic_values}
\title{Clean Raw MIC Values}
\usage{
amrc_clean_mic_values(
  x,
  less_than = c("numeric", "half"),
  greater_than = c("numeric", "double")
)
}
\arguments{
\item{x}{A vector of raw MIC values.}

\item{less_than}{How to interpret \code{<} and \code{<=} values: keep the reported
number (\code{"numeric"}) or divide it by two (\code{"half"}).}

\item{greater_than}{How to interpret \code{>} and \code{>=} values: keep the reported
number (\code{"numeric"}) or multiply it by two (\code{"double"}).}
}
\value{
A numeric vector.
}
\description{
Parses common raw MIC strings into numeric values. This is useful for
generic MIC tables that include censoring prefixes such as \code{<}, \code{<=}, \code{>},
or \code{>=} before a later log transform.
}
\details{
By default the function strips the qualifier and keeps the reported numeric
value. If you want to interpret censored values as one dilution lower or
higher, use \code{less_than = "half"} and/or \code{greater_than = "double"}.
}
