% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mic_data.R
\name{amrc_compute_sequence_distance}
\alias{amrc_compute_sequence_distance}
\title{Compute a Distance Matrix from Aligned Sequence or Allele Data}
\usage{
amrc_compute_sequence_distance(
  data,
  id_col = NULL,
  sequence_cols = NULL,
  metadata_cols = NULL,
  drop_incomplete = TRUE,
  allow_duplicate_ids = FALSE,
  method = "pairwise",
  pairwise.deletion = FALSE,
  variance = FALSE
)
}
\arguments{
\item{data}{An \code{amrc_external_data} object with \code{feature_mode = "character"},
or a \code{data.frame}/matrix containing one row per isolate and one column per
aligned sequence or allele position.}

\item{id_col}{Name of the isolate identifier column when \code{data} is a
\code{data.frame}.}

\item{sequence_cols}{Character vector naming the aligned sequence or allele
columns when \code{data} is a \code{data.frame}. When \code{NULL}, all non-identifier,
non-metadata columns are used.}

\item{metadata_cols}{Optional metadata columns to ignore when inferring
\code{sequence_cols}.}

\item{drop_incomplete}{Logical; drop rows with incomplete aligned states
before distance computation.}

\item{allow_duplicate_ids}{Logical; allow duplicate isolate identifiers when
\code{TRUE}.}

\item{method}{Distance method passed to \code{\link[ape:dist.gene]{ape::dist.gene()}}.}

\item{pairwise.deletion}{Logical; passed to \code{\link[ape:dist.gene]{ape::dist.gene()}}.}

\item{variance}{Logical; passed to \code{\link[ape:dist.gene]{ape::dist.gene()}}.}
}
\value{
A \code{dist} object aligned to isolate IDs.
}
\description{
Builds an external distance structure from an aligned character-state table
using \code{\link[ape:dist.gene]{ape::dist.gene()}}. This is the generic route for genotype-style
inputs when users have one row per isolate and one column per aligned
sequence or allele position.
}
\details{
If your non-MIC structure is already summarised as numeric coordinates or a
precomputed distance matrix, prefer \code{\link[=amrc_compute_external_feature_distance]{amrc_compute_external_feature_distance()}}
or \code{\link[=amrc_compute_external_distance]{amrc_compute_external_distance()}}. If your raw genotype data still need
organism-specific parsing and cleaning before they become an aligned table,
do that preprocessing first and then pass the aligned result here.
}
