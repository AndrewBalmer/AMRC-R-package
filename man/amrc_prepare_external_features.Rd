% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mic_data.R
\name{amrc_prepare_external_features}
\alias{amrc_prepare_external_features}
\title{Prepare Generic External or Genotype Feature Data}
\usage{
amrc_prepare_external_features(
  data,
  id_col,
  feature_cols = NULL,
  metadata = NULL,
  metadata_id_col = id_col,
  metadata_cols = NULL,
  join = c("left", "inner", "none"),
  feature_mode = c("numeric", "character"),
  exclude_ids = NULL,
  normalise_strings = TRUE,
  drop_incomplete = TRUE,
  allow_duplicate_ids = FALSE,
  fill_tables = FALSE,
  reader = utils::read.csv,
  ...
)
}
\arguments{
\item{data}{A data frame, matrix, file path, or list of such objects.}

\item{id_col}{Name of the isolate identifier column in the external feature
table.}

\item{feature_cols}{Optional character vector naming feature columns.}

\item{metadata}{Optional metadata \code{data.frame} to align against.}

\item{metadata_id_col}{Identifier column in \code{metadata}.}

\item{metadata_cols}{Optional metadata columns to retain. When \code{NULL}, all
metadata columns are kept.}

\item{join}{How to combine \code{metadata} with the external feature table.
\code{"left"} keeps metadata order, \code{"inner"} keeps shared IDs only, and
\code{"none"} ignores \code{metadata}.}

\item{feature_mode}{Passed to \code{\link[=amrc_standardise_external_data]{amrc_standardise_external_data()}}.}

\item{exclude_ids}{Optional character vector of isolate identifiers to drop.}

\item{normalise_strings}{Logical; convert blanks to \code{NA} and common
TRUE/FALSE spellings to \code{T}/\code{F}.}

\item{drop_incomplete}{Logical; drop rows with incomplete features.}

\item{allow_duplicate_ids}{Logical; allow duplicate isolate identifiers.}

\item{fill_tables}{Logical; when \code{TRUE}, allow bound tables with different
columns.}

\item{reader}{Function used to read file-path inputs.}

\item{...}{Additional arguments passed to \code{reader}.}
}
\value{
An object of class \code{amrc_external_data}.
}
\description{
Convenience wrapper for common raw genotype/external preprocessing patterns:
reading and binding multiple feature tables, normalising logical-like strings
such as \code{TRUE}/\code{FALSE}, optionally aligning the feature table to a metadata
table, excluding selected isolate identifiers, and returning a standardised
object of class \code{amrc_external_data}.
}
\details{
This is the generic replacement for the useful parsing behaviour that had
previously been embedded inside organism-specific preprocessing scripts.
}
