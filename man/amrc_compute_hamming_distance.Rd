% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mic_data.R
\name{amrc_compute_hamming_distance}
\alias{amrc_compute_hamming_distance}
\title{Compute a Hamming-Style Distance Matrix from Aligned Character Features}
\usage{
amrc_compute_hamming_distance(
  data,
  id_col = NULL,
  feature_cols = NULL,
  metadata_cols = NULL,
  normalise = TRUE,
  drop_incomplete = TRUE,
  allow_duplicate_ids = FALSE
)
}
\arguments{
\item{data}{An \code{amrc_external_data} object with \code{feature_mode = "character"},
or a \code{data.frame}/matrix containing one row per isolate and one column per
aligned sequence, allele, or character-state position.}

\item{id_col}{Name of the isolate identifier column when \code{data} is a
\code{data.frame}.}

\item{feature_cols}{Character vector naming the aligned character-state
columns when \code{data} is a \code{data.frame}. When \code{NULL}, all non-identifier,
non-metadata columns are used.}

\item{metadata_cols}{Optional metadata columns to ignore when inferring
\code{feature_cols}.}

\item{normalise}{Logical; divide mismatch counts by the number of feature
columns when \code{TRUE}.}

\item{drop_incomplete}{Logical; drop rows with incomplete aligned states
before distance computation.}

\item{allow_duplicate_ids}{Logical; allow duplicate isolate identifiers when
\code{TRUE}.}
}
\value{
A \code{dist} object aligned to isolate IDs.
}
\description{
Builds an external distance structure from an aligned character-state or
allele table by counting per-column mismatches. This is the generic
Hamming-style route for genotype-like inputs when users have one row per
isolate and one column per aligned state.
}
\details{
If you want the \code{ape::dist.gene()} behaviour used in the original genotype
workflow, prefer \code{\link[=amrc_compute_sequence_distance]{amrc_compute_sequence_distance()}}. Use this helper when a
plain mismatch/Hamming summary is the distance you want.
}
