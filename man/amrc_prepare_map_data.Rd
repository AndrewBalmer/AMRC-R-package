% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/comparison_analysis.R
\name{amrc_prepare_map_data}
\alias{amrc_prepare_map_data}
\title{Prepare a Generic Phenotype/External Map Comparison Table}
\usage{
amrc_prepare_map_data(
  metadata,
  phenotype_mds,
  external_mds,
  id_col,
  group_col = NULL,
  group_output_col = group_col,
  phenotype_rotation_degrees = NULL,
  external_rotation_degrees = NULL,
  exclude_ids = NULL,
  phenotype_coord_names = c("D1", "D2"),
  external_coord_names = c("E1", "E2"),
  centroid_coord_names = paste0(phenotype_coord_names, "_centroid")
)
}
\arguments{
\item{metadata}{A metadata \code{data.frame} containing at least the isolate ID
column and any optional grouping columns.}

\item{phenotype_mds}{Phenotype MDS fit.}

\item{external_mds}{External MDS fit, for example from a genotype or
phylogenetic distance structure.}

\item{id_col}{Name of the isolate identifier column in \code{metadata}.}

\item{group_col}{Optional grouping column used to compute centroid summaries,
for example lineage or strain type.}

\item{group_output_col}{Optional output name for \code{group_col}. Defaults to the
same value as \code{group_col}.}

\item{phenotype_rotation_degrees}{Optional rotation applied to the phenotype
map after calibration.}

\item{external_rotation_degrees}{Optional rotation applied to the external
map after calibration.}

\item{exclude_ids}{Optional character vector of isolate identifiers to
remove after alignment.}

\item{phenotype_coord_names}{Output names for the phenotype coordinates.}

\item{external_coord_names}{Output names for the external coordinates.}

\item{centroid_coord_names}{Output names for group-centroid columns computed
from the phenotype coordinates. Defaults to \code{paste0(phenotype_coord_names, "_centroid")}.}
}
\value{
A list with the combined isolate-level \code{data}, optional distinct
\code{group_data}, and the phenotype/external calibration objects.
}
\description{
Calibrates one phenotype map and one external map onto interpretable scales,
aligns them to a shared metadata table, and returns a combined comparison
object that downstream clustering, plotting, and summary helpers can reuse.
}
\details{
This is the generic comparison-preparation entry point. Organism-specific
helpers such as \code{\link[=amrc_prepare_spneumoniae_map_data]{amrc_prepare_spneumoniae_map_data()}} should wrap this
function rather than reimplementing the join logic themselves.
}
