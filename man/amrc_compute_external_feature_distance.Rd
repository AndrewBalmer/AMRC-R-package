% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mic_data.R
\name{amrc_compute_external_feature_distance}
\alias{amrc_compute_external_feature_distance}
\title{Compute an External Distance Matrix from Feature Data}
\usage{
amrc_compute_external_feature_distance(
  data,
  feature_cols = NULL,
  id_col = NULL,
  feature_mode = c("numeric", "character"),
  normalise_mismatch = TRUE,
  method = "euclidean",
  ...
)
}
\arguments{
\item{data}{A \code{data.frame} or matrix containing one row per isolate.}

\item{feature_cols}{Optional character vector naming the feature columns to
use when \code{data} is a \code{data.frame}. When \code{NULL}, all non-identifier columns
are used.}

\item{id_col}{Optional isolate identifier column name when \code{data} is a
\code{data.frame}.}

\item{feature_mode}{Either \code{"numeric"} or \code{"character"}. When \code{data} is an
\code{amrc_external_data} object, the stored feature mode is used.}

\item{normalise_mismatch}{Logical; when computing distances from character
features, divide mismatch counts by the number of feature columns.}

\item{method}{Distance method passed to \code{\link[stats:dist]{stats::dist()}}.}

\item{...}{Additional arguments passed to \code{\link[stats:dist]{stats::dist()}}.}
}
\value{
A labelled \code{dist} object when isolate identifiers are available.
}
\description{
Builds a distance matrix from a generic external feature table, for example
genotype-derived summary features, phylogenetic embeddings, or any other
aligned non-MIC measurements.
}
