% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/phenotype.R
\name{amrc_process_spneumoniae_phenotype}
\alias{amrc_process_spneumoniae_phenotype}
\title{Process the S. pneumoniae Phenotype Example Data}
\usage{
amrc_process_spneumoniae_phenotype(
  input_dir = file.path("data-raw", "raw-data", "spneumoniae"),
  mic_files = c("MIC_pneumo.csv", "MIC_pneumo2.csv"),
  drop_incomplete = TRUE,
  compute_distance = TRUE,
  sample_n = NULL,
  seed = 1234,
  save_outputs = FALSE,
  out_dir = file.path("inst", "extdata", "generated", "spneumoniae"),
  save_legacy_rdata = TRUE
)
}
\arguments{
\item{input_dir}{Directory containing the raw phenotype CSV files.}

\item{mic_files}{Length-2 character vector of phenotype file names.}

\item{drop_incomplete}{Logical; drop rows with missing MIC values.}

\item{compute_distance}{Logical; compute \code{\link[stats:dist]{stats::dist()}} on the processed MIC
table when \code{TRUE}.}

\item{sample_n}{Optional integer. When supplied, compute the distance matrix
on a reproducible random subsample instead of the full table.}

\item{seed}{Integer seed used when \code{sample_n} is supplied.}

\item{save_outputs}{Logical; write processed files to \code{out_dir} when \code{TRUE}.}

\item{out_dir}{Directory for optional saved outputs.}

\item{save_legacy_rdata}{Logical; additionally save legacy \code{.RData} outputs
expected by the original notebooks when \code{TRUE}.}
}
\value{
A named list with \code{metadata}, \code{mic}, and \code{distance}.
}
\description{
Reads the two phenotype CSV files used in the legacy input script, combines
them, repairs the PBP-type metadata, log2-transforms MIC values, and
optionally computes a phenotype distance matrix.
}
\details{
This is an example-specific wrapper retained for the packaged
\verb{S. pneumoniae} case study. Generic MIC workflows should prefer
\code{\link[=amrc_standardise_mic_data]{amrc_standardise_mic_data()}} and \code{\link[=amrc_compute_mic_distance]{amrc_compute_mic_distance()}}.

Lifecycle note: this function remains exported for case-study compatibility,
but it is not intended to define the long-term generic package API. In the
\verb{0.1.0} public milestone it should be read as a supported case-study helper,
not as the recommended entry point for new analyses.
}
